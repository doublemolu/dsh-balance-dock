# Changelog

All notable changes to dsh-balance-dock.

## [1.1.2] - 2026

### Added
- **设置弹窗**：充值按钮右侧新增"设置 ⚙"按钮，点击弹出可视化设置窗口：
  - 可编辑：每段金额 / 红色阈值 / 黄色警告 / 红色警告 / 汇率 / 两个模型的价目
  - 底部三按钮：恢复默认（回到开发版默认值）/ 取消 / 确认（保存后约 10s 生效）
- **刷新动效**：顶部 ⟳ 按钮点击时旋转 360°。

## [1.1.1] - 2026

### Changed
- "本次使用模型"拆分为独立一行，显示在"本次用量"下方。

## [1.1.0] - 2026

### Added
- **可配置化**：新增 `$DSH_HOME/dsh-balance-dock.json` 配置文件（首次启动自动生成），可调整：
  - `segmentBase`：进度条每段金额（默认 50）
  - `redThreshold`：最右段变红阈值（默认 15）
  - `warnLow` / `warnDanger`：低余额提醒阈值（默认 10 / 3）
  - `fxCny`：人民币近似汇率（默认 7.1）
  - `prices`：模型价目表（USD/百万 token）
- **显示明细**：
  - "本次用量"行显示具体模型名
  - "本会话累计"行悬停显示按模型拆分的用量/费用明细
- **交互增强**：
  - 卡片标题旁新增手动刷新按钮（⟳）
  - 卡片底部显示"更新于 HH:MM:SS"
  - 余额变化时显示 ▲/▼ 变化提示（10 秒后消失）
- **代码清理**：移除调试路由 `/dsh-balance/debug`

### Changed
- **Host 端不再使用子进程**：改用 Node 原生 `fetch` 直连余额接口，彻底消除 `spawn EINVAL` 一类问题，也更省资源。
- 阈值/段大小等全部读取配置（未配置时使用默认值）。

### Fixed
- （v1.0.0 已修复，随本版保留）`spawn EINVAL`：`node` 被解析为 `.cmd` 壳导致子进程无法启动。

## [1.0.0] - 2026

首个稳定发布。

### Added
- 侧边栏余额卡片：余额 / 赠金 / 充值，15s 自动刷新（免费接口，不消耗 token）
- 本会话花费：
  - 本次用量（最近一次模型调用的 tokens + 估算费用）
  - 本会话累计（从持久会话日志折叠，重启不丢，按真实模型 + 峰谷折扣计价）
  - 本会话累计调用模型次数
- 50 元分段进度条：轨道 = 充值金额，绿色填充 = 剩余余额；从右端消耗；最右段低于 ¥15 变红；段耗尽时"气球爆裂"特效 + 平滑过渡
- 充值按钮：点击打开 DeepSeek 开放平台充值页
- 低余额提醒（< ¥10 黄灯、< ¥3 红灯、"会话花费超余额"提示）
- 位置守护：其他插件抢位时弹窗确认
- 主题适配（DSH theme tokens，深/浅色自适应）
- 双语文档（README.md / README.zh.md）
