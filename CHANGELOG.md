# Changelog

All notable changes to dsh-balance-dock.

## [1.2.2] - 2026

### Changed
- 四个信息行标签（本次用量 / 本次使用模型 / 本会话累计 / 调用次数）**恢复默认颜色**（移除蓝色加粗）。
- 赠金金额**恢复默认颜色**；最上方余额大金额保留金黄色。
- **充值显示重做**：改为两行——"最近充值"（最近一次充值的时间 + 金额，仅在有新充值时变化）+ "累计充值"（所有充值之和）。

### Added
- **充值本地追踪**：官方 API 不提供充值历史，插件通过观测充值余额增长自动记录每次充值（时间 + 金额，持久化到本地文件，重启不丢），从安装后开始记录。

## [1.2.1] - 2026

### Fixed
- **蓝色标签修复**：`--dsw-alias-brand-primary` 在该主题中实际为中性色（非蓝色），改用真正的 DeepSeek 蓝 `--dsw-static-deepseek-450`。
- **请求失败显式报错**：余额请求失败时卡片现在会显示具体错误文本（此前静默失败）。

## [1.2.0] - 2026

### Added
- **多语言**（7 种）：简体中文 / 繁體中文 / English / Deutsch / 日本語 / 한국어 / Русский，设置弹窗一键切换，卡片与位置守护弹窗全部跟随。
- **货币选择**：CNY / TWD / USD / EUR / JPY / KRW / RUB，随语言自动切换默认货币，也可手动指定；金额按汇率换算后以当地格式显示（如 ¥、$、€、₩、₽）。
- **汇率自动更新**：从免费公开汇率 API（open.er-api.com，以人民币为基准）自动拉取，6 小时缓存，**不消耗任何模型 token**；失败时回退到内置兜底汇率。
- **金色金额 + 蓝色标签**：余额金额显示为金黄色；"本次用量 / 本次使用模型 / 本会话累计 / 本会话累计调用模型次数"标签改为蓝色加粗。

## [1.1.3] - 2026

### Changed
- **设置弹窗移除模型价目编辑**（价目仍可通过配置文件调整）。
- **新增显示行勾选**：设置弹窗可勾选/取消"本次用量 / 本次使用模型 / 本会话累计 / 本会话累计调用模型次数"，取消勾选后对应行在卡片中隐藏，布局自适应。

## [1.1.2] - 2026

### Added
- **设置弹窗**：充值按钮右侧新增"设置 ⚙"按钮，点击弹出可视化设置窗口：
  - 可编辑：每段金额 / 红色阈值 / 黄色警告 / 红色警告 / 汇率 / 两个模型的价目
  - 底部三按钮：恢复默认（回到开发版默认值）/ 取消 / 确认（保存后约 10s 生效）
- **刷新动效**：顶部 ⟳ 按钮点击时旋转 360°。

## [1.1.1] - 2026

### Changed
- "本次使用模型"拆分为独立一行，显示在"本次用量"下方。

## [1.1.0] - 2026

### Added
- **可配置化**：新增 `$DSH_HOME/dsh-balance-dock.json` 配置文件（首次启动自动生成），可调整：
  - `segmentBase`：进度条每段金额（默认 50）
  - `redThreshold`：最右段变红阈值（默认 15）
  - `warnLow` / `warnDanger`：低余额提醒阈值（默认 10 / 3）
  - `fxCny`：人民币近似汇率（默认 7.1）
  - `prices`：模型价目表（USD/百万 token）
- **显示明细**：
  - "本次用量"行显示具体模型名
  - "本会话累计"行悬停显示按模型拆分的用量/费用明细
- **交互增强**：
  - 卡片标题旁新增手动刷新按钮（⟳）
  - 卡片底部显示"更新于 HH:MM:SS"
  - 余额变化时显示 ▲/▼ 变化提示（10 秒后消失）
- **代码清理**：移除调试路由 `/dsh-balance/debug`

### Changed
- **Host 端不再使用子进程**：改用 Node 原生 `fetch` 直连余额接口，彻底消除 `spawn EINVAL` 一类问题，也更省资源。
- 阈值/段大小等全部读取配置（未配置时使用默认值）。

### Fixed
- （v1.0.0 已修复，随本版保留）`spawn EINVAL`：`node` 被解析为 `.cmd` 壳导致子进程无法启动。

## [1.0.0] - 2026

首个稳定发布。

### Added
- 侧边栏余额卡片：余额 / 赠金 / 充值，15s 自动刷新（免费接口，不消耗 token）
- 本会话花费：
  - 本次用量（最近一次模型调用的 tokens + 估算费用）
  - 本会话累计（从持久会话日志折叠，重启不丢，按真实模型 + 峰谷折扣计价）
  - 本会话累计调用模型次数
- 50 元分段进度条：轨道 = 充值金额，绿色填充 = 剩余余额；从右端消耗；最右段低于 ¥15 变红；段耗尽时"气球爆裂"特效 + 平滑过渡
- 充值按钮：点击打开 DeepSeek 开放平台充值页
- 低余额提醒（< ¥10 黄灯、< ¥3 红灯、"会话花费超余额"提示）
- 位置守护：其他插件抢位时弹窗确认
- 主题适配（DSH theme tokens，深/浅色自适应）
- 双语文档（README.md / README.zh.md）
